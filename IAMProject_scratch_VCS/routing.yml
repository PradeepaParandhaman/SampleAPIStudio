kind: Route
apiVersion: api.webmethods.io/beta
metadata:
  name: Routing
  version: '1.0'
  namespace: dev
  tags:
    - policySeq
    - test
spec:
  default-endpoint:
    $ref: dev:test_endpoint:alpha
