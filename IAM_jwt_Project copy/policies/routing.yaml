#
# Copyright Super iPaaS Integration LLC, an IBM Company 2024
#
kind: Route
apiVersion: api.webmethods.io/beta
metadata:
  name: routing
  version: alpha
  namespace: iam_all
  tags:
    - routing
    - route
    - policy
spec:
  default-endpoint:
    $ref: iam_all:endpoint:alpha