#
# Copyright Super iPaaS Integration LLC, an IBM Company 2024
#
kind: PolicySequence
apiVersion: api.webmethods.io/beta
metadata:
  name: policyseq
  version: 1.0
  namespace: dev
  tags:
    - seq
spec:
  transport:
    - $ref: dev:transport:alpha
  routing:
    - $ref: dev:routing:1.0